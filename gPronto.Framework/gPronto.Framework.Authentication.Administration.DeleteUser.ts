export function gProntoFrameworkAuthenticationDeleteUser(
  _authUserId: string,
): never {
  throw new Error(
    "Authentication.Delete.User.Function is not implemented. A trusted server boundary is required.",
  );
}
